
下面是兩個 SVG 中，一個是原本透過 GH Action 生成的 SVG 表格，另一個是經過我手動修改後的 SVG 表格：



我發現進行了以下改後，就能夠顯示我所預期的效果：

- 修改 `<svg>` 標記中的 `width="120"` 為 `width="960"`
- 修改 `<svg>` 標記中的 `viewBox="0 0 120 160"` 為 `viewBox="0 0 960 160"`

你覺得如何？請問能否以此為參考改善目前的 GH Action？



Below are two SVGs: the former is the original SVG table generated by the GH Action, and the latter is the SVG table I manually modified:

I found that after making the following changes, it displays the effect I expected:

- Modify `width="120"` to `width="960"` in the `<svg>` tag
- Modify `viewBox="0 0 120 160"` to `viewBox="0 0 960 160"` in the `<svg>` tag

What do you think? Could you improve the current GH Action based on this reference?

